language: "python"

python:
  - "3.8"
  
branches:
  only:
    - next

install:
  - pip install --upgrade pip
  - pip install -r requirements-dev.txt

before_script:
  - sudo chmod +x scripts/format.sh

script:
  - scripts/format.sh
